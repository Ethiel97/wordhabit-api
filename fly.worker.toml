app = "wordhabit-worker"
primary_region = "yyz"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "3000"

[processes]
  worker = "pnpm run worker"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 512