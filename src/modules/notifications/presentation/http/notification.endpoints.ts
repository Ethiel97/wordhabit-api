/** Paths are relative to BASE, which the controller already mounts. */
export const NOTIFICATIONS = {
  BASE: 'notifications',
  GET_PREFERENCES: 'preferences',
  UPDATE_PREFERENCES: 'preferences/:code',
  REGISTER_DEVICE: 'devices',
  DELETE_DEVICE: 'devices/:token',
};
