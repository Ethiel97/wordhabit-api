/**
 * Longer than the sign-up code's fifteen minutes: this one arrives at an
 * address the user has just typed and may not have open yet.
 */
export const EMAIL_CHANGE_CODE_TTL_MINUTES = 30;

export const MAX_EMAIL_CHANGE_ATTEMPTS = 5;
